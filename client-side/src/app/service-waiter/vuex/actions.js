import * as types from './mutations-types'

export const attemptPayment = (context, payload) => {
  // const { itemSelected, addPayment } = payload
  // console.log('actions', payload)
  // context.commit(types.PAYMENT, payload)
}
